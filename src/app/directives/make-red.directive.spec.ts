import { MakeRedDirective } from './make-red.directive';

describe('MakeRedDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeRedDirective();
    expect(directive).toBeTruthy();
  });
});
